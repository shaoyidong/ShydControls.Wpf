<Window
    x:Class="ShydControls.Wpf.Demo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ShydControls.Wpf.Demo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow" Loaded="Window_Loaded"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid Background="Black" TextElement.Foreground="White">
        <Grid.Resources>
            <Style TargetType="{x:Type TextElement}">
                <Setter Property="Foreground" Value="White" />
            </Style>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="White" />
            </Style>
            <Style TargetType="Label">
                <Setter Property="Foreground" Value="White" />
            </Style>
            <Style TargetType="RadioButton">
                <Setter Property="Foreground" Value="White" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel TextElement.Foreground="White">

            <RadioButton Click="ConventionalBtn_Click" Content="常规" />
            <RadioButton Click="ApertureBtn_Click" Content="光圈" />
        </StackPanel>
        <ContentControl x:Name="contentControl" Grid.Column="1" />
    </Grid>
</Window>
