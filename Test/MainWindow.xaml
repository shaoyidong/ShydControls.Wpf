<Window
    x:Class="Test.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrs="clr-namespace:ShydControls.Wpf;assembly=ShydControls.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Test"
    xmlns:mbeCtrs="clr-namespace:ShydControls.Wpf.IndustrialComponents;assembly=ShydControls.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="1469"
    Height="700"
    mc:Ignorable="d">
    <Grid>
        <mbeCtrs:MbeInstituteOfTechnicalPhysics3Inches x:Name="v" />
        <Button
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Click="Button_Click"
            Content="MainShutter" />
        <!--<StackPanel>
            <TextBox x:Name="txt" />

            <ctrs:NumbericTextBox
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Text="{Binding ElementName=txt, Path=Text}" />
        </StackPanel>-->

    </Grid>
</Window>
